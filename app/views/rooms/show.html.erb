<% provide(:title, @user.name, @room.name, @room.id) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        Räume
      </h1>
      <p>
        in der Messestadt
      </p>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @user.rooms.any? %>
      <h3>Räume ( <%= @user.rooms.count %> )</h3>
      <ol class="rooms">
        <%= render @rooms %>
      </ol>
      <%= will_paginate @rooms %>
    <% end %>
  </div>
</div>
